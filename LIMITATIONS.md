# LIMITATIONS · 当前已知限制

本文件记录 PsychAI 目前存在的已知限制。这些限制是真实存在的，不会被隐瞒。大部分限制将在后续版本中陆续解决。

---

## Option A · Skill 版

### Windows vs macOS 功能差异

| 功能 | Windows 版 | macOS 版 |
|------|-----------|---------|
| WeFlow 微信解密集成 | ✅ 支持 | ❌ 暂不支持 |
| WeChat 聊天记录自动读取 | ✅ | 需手动导出后放入 input/ |
| Python 探测 | `python` 命令 | `python3` 命令 |
| 整体完备程度 | 完备版 | 试运行版 |

### Skill 形态的根本限制

以下限制来自 Claude Code Skill 的机制本身，无法在当前形态下根本解决，计划在未来独立 CLI 版本中处理：

1. **LLM 可能跳步**：Skill 依靠提示词约束 LLM 行为，不是代码层面的强制执行。AI 在极少数情况下可能跳过某些步骤。

2. **Windows 第 0 步依赖 PowerShell**：Python 探测和目录创建使用 PowerShell 语法。在 cmd 或 Git Bash 环境下运行可能失败。

3. **跨平台 shell 假设**：macOS 版的 bash 语法在某些 zsh 配置下可能出现细节差异。

4. **文件路径使用绝对路径 key**：`files_analyzed` 字典以绝对路径为 key。如果用户将工作目录移动到其他位置，所有已记录的文件会被重新识别为"新文件"，触发重复分析。

5. **WeFlow 仅 Windows**：Mac 端微信数据解密方案尚未验证，Mac 用户目前只能手动导出聊天记录后放入 `input/wechat/`。

---

## Option B · 提示词版

1. **受限于 AI 平台的上下文窗口**：提供大量材料时，超出上下文窗口的内容会被截断。付费版 AI 通常有更长的上下文窗口。

2. **跨对话无持久记忆**：快照需要手动复制粘贴，无法自动续写。这是 Option A 的核心优势之一。

3. **无法强制分析顺序**：提示词约束不如代码层面可靠，AI 偶尔可能简化某些步骤。

---

## 两个版本共有

1. **分析仅基于你提供的材料**：PsychAI 只能看到你愿意分享的内容，不能替代面对面的专业评估。

2. **不能诊断心理疾病**：所有分析均为参考性质，不具备医疗诊断效力。

---

*如果你遇到了本文档未记录的问题，请通过 GitHub Issues 反馈。*
